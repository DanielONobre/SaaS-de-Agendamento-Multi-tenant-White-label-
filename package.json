{
  "name": "saas-scheduler",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=!//",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}